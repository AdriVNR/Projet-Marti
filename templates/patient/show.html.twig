{% extends 'base.html.twig' %}

{% block title %}Patient{% endblock %}

{% block body %}
    <h1>Patient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ patient.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ patient.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ patient.age }}</td>
            </tr>
        </tbody>
    </table>

    <a id="backtl" href="{{ path('patient_index') }}">back to list</a>

    <button class="btn btn-warning"><a id="white" href="{{ path('patient_edit', {'id': patient.id}) }}">edit</a></button>

    {{ include('patient/_delete_form.html.twig') }}
{% endblock %}
