{% extends 'base.html.twig' %}

{% block title %}Reporting{% endblock %}

{% block body %}
    <h1>Reporting</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reporting.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ reporting.value }}</td>
            </tr>
        </tbody>
    </table>

    <button id="backtl"><a href="{{ path('reporting_index') }}">back to list</a></button>

    <a href="{{ path('reporting_edit', {'id': reporting.id}) }}">edit</a>

    {{ include('reporting/_delete_form.html.twig') }}
{% endblock %}
